import 'package:flutter/material.dart';

class AppConstants {
  static const String appName = 'GARD CONFIG SCANNER';
  static const String appVersion = 'v1.0.0-Ultimate';
  
  // Colors
  static const Color primaryBg = Color(0xFF05070B);
  static const Color cardBg = Color(0xFF0E1420);
  static const Color accentNeon = Color(0xFF00E676);
  static const Color textMuted = Color(0 deeds8C9AA);
  
  // Default Scanner Settings
  static const int defaultTimeout = 2500; // ms
  static const int defaultConcurrency = 50;
}
